$(document).ready(function () {
    //$('#myModal').on('show.bs.modal', function (e) {
    //    var productId = $(e.relatedTarget).data('prod-id');
    //    $.get('/product/' + productId, function (data) {
    //        $("#productId").val(productId);
    //        console.log(data);
    //        $("#ProductName").val(data.ProductName);
    //    });
    //});
});